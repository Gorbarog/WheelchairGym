import SwiftUI

enum TabBarLayout {
    /// Extra bottom padding for `ScrollView` content inside `TabView` so the last rows are not covered by the tab bar and home indicator.
    static let scrollContentBottomPadding: CGFloat = 100
}
